version: 0.2
phases:
  pre_build:
    Commands:
    - echo "Entered the pre-build phase"
  build:
    Commands:
    - echo "Entered bulid phase"
    - mvn clean prepare-package war:exploded
  post_build:
    Commands:
    - echo "enter the post bulid phase"
    - mv target/deploy ./
artifacts:
  files:
    -deploy/**/*